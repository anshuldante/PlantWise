<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Media access for gallery picker on Android 13+ -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />

    <!-- Notifications for care reminders (Phase 7 - infrastructure only) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Exact alarms for care reminder scheduling (Phase 7 - infrastructure only) -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

    <!-- Boot receiver for rescheduling alarms after device restart -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:name=".LeafIQApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LeafIQ">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.camera.CameraActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.LeafIQ" />

        <activity
            android:name=".ui.analysis.AnalysisActivity"
            android:exported="false"
            android:theme="@style/Theme.LeafIQ" />

        <activity
            android:name=".ui.detail.PlantDetailActivity"
            android:exported="false"
            android:theme="@style/Theme.LeafIQ" />

        <activity
            android:name=".ui.timeline.AnalysisDetailActivity"
            android:parentActivityName=".MainActivity"
            android:exported="false"
            android:theme="@style/Theme.LeafIQ" />

        <activity
            android:name=".ui.diagnosis.QuickDiagnosisActivity"
            android:exported="false"
            android:theme="@style/Theme.LeafIQ" />

        <!-- BroadcastReceiver for care reminder alarms and notification actions -->
        <receiver android:name=".care.CareReminderReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.leafiq.app.ACTION_CARE_DONE"/>
                <action android:name="com.leafiq.app.ACTION_CARE_SNOOZE"/>
            </intent-filter>
        </receiver>

        <!-- BroadcastReceiver for rescheduling alarms after device reboot -->
        <receiver android:name=".care.BootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
            </intent-filter>
        </receiver>

    </application>

</manifest>
